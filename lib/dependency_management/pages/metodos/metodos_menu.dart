import 'package:flutter/material.dart';

class MetodosMenu extends StatelessWidget {

  const MetodosMenu({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Menu de MÉTODOS'),),
           body: Container(),
       );
  }
}