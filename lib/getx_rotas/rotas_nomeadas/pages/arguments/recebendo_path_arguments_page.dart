import 'package:flutter/material.dart';

class RecebendoPathArgumentsPage extends StatelessWidget {

  const RecebendoPathArgumentsPage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Recebendo PATH arguments'),),
           body: Container(),
       );
  }
}