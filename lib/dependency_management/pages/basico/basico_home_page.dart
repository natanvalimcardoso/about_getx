import 'package:flutter/material.dart';

class BasicoHomePage extends StatelessWidget {

  const BasicoHomePage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}