import 'package:flutter/material.dart';

class RecebendoQueryArgumentsPage extends StatelessWidget {

  const RecebendoQueryArgumentsPage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Recebendo querry arguments'),),
           body: Container(),
       );
  }
}