import 'package:flutter/material.dart';

class PageNomeadaUm extends StatelessWidget {

  const PageNomeadaUm({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Pagina UM'),),
           body: Container(),
       );
  }
}